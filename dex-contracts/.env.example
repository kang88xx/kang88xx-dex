# --- Xphere deployment config ---
# JSON-RPC endpoint for the Xphere chain
RPC_URL=

# Deployer private key (hex, with or without 0x). NEVER commit the real value.
PRIVATE_KEY=

# Optional: Xphere chain id. If omitted, Hardhat will not pin chainId.
XPHERE_CHAIN_ID=
